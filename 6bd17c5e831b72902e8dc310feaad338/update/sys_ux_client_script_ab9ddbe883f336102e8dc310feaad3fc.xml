<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ux_client_script">
    <sys_ux_client_script action="INSERT_OR_UPDATE">
        <controller_preset/>
        <includes/>
        <macroponent display_value="Default">479ddbe883f336102e8dc310feaad3be</macroponent>
        <name>On Gen AI Data Fetch</name>
        <preset/>
        <required_translations>[
    {
        "message": "Information from the following record(s) is used: {0}. Check AI-generated content for accuracy."
    },
    {
        "message": "There was a problem generating this article with Now Assist. You can write it yourself now if you'd like."
    }
]</required_translations>
        <script><![CDATA[/**
 * @param {params} params
 * @param {api} params.api
 * @param {any} params.event
 * @param {any} params.imports
 * @param {ApiHelpers} params.helpers
 */
async function handler({
    api,
    event,
    helpers,
    imports
}) {
    if (event.name === "DATA_FETCH_SUCCEEDED") {
        if (api.state.selectedLanguage) {
            const languageTimeout = await helpers.timing.setTimeout(function() {
                api.emit('GEN_AI_SET_FIELD', {
                    fieldName: 'language',
                    value: api.state.selectedLanguage
                });
            }, 2500);
        }
        if (api.state.isGenAiTimedOut) {
            // TODO: send Gen AI feedback = Ignored
            return;
        }
        var output = api.data.generate_article_content_with_gen_ai_1.output;

        if (output.success) {
            if (output.isAsync) {
                api.setState('asyncResponseId', output.content);
                api.setState('subscribeAsyncResponse', true);
            } else {
                api.setState('isGenAiGenerating', false);
                helpers.timing.clearTimeout(api.state.genAiTimeoutId);
                var response = output.content;
                if (response.success) {
                    for (var key in response.fields) {
                        api.emit('GEN_AI_SET_FIELD', {
                            fieldName: key,
                            value: response.fields[key]
                        });
                    }
                    var selectedRecord = api.state.genAiSourceRecordId;
                    if (!selectedRecord && api.state.filteredSelectedTasks) {
                        selectedRecord = Object.keys(api.state.filteredSelectedTasks)[0]; 
                    }
                    function getTaskNumber() { 
                        const task = api.state.filteredSelectedTasks?.[selectedRecord]; 
                        return task?.cells?.number?.value || api.state.taskNumber; 
                    }

                    var taskNumber = getTaskNumber();
                    
                    var content = await helpers.translate("Information from the following record(s) is used: {0}. Check AI-generated content for accuracy.", taskNumber);

                    api.emit("NOW_UXF_PAGE#ADD_NOTIFICATIONS", {
                        items: [{
                            id: "na_drafted",
                            status: "moderate",
                            icon: "ai-sparkle-fill",
                            content: {
                                type: "html",
                                value: content
                            },
                            action: {
                                type: "dismiss"
                            }
                        }]
                    });
                    api.setState('isGenAiGenerated', true);
                    
                    api.setState('telemetryPayload', {
                        event_key: 'taskList',
                        targetTableSysId: api.data.record.sysId,
                        selectedRecords: [selectedRecord]
                    });
                    api.setState('isTelemetryExecuting', true);
                    helpers.timing.setTimeout(function() {
                        api.setState('telemetryPayload', {
                            event_key: 'isGeneratedFromCustomTemplateFramework',
                            targetTableSysId: api.data.record.sysId,
                            sourceTable: api.state.sourceTable,
                            targetTable: api.state.targetTable
                        });
                        api.setState('isTelemetryExecuting', true);
                    }, 1000);

                } else {
                    api.emit("NOW_UXF_PAGE#ADD_NOTIFICATIONS", {
                        items: [{
                            id: "na_error",
                            status: "critical",
                            icon: "ai-sparkle-fill",
                            content: response.errorMessage,
                            action: {
                                type: "dismiss"
                            }
                        }]
                    });
                }
                api.emit("NOW_UXF_PAGE#REMOVE_NOTIFICATIONS", {
                    items: [{
                        id: "na_drafting"
                    }]
                });
                helpers.modal.close("modal_gen_ai");
            }
        } else {
            // This branch of code should be empty since the "generate" databroker will be executed immediately on load
            // Therefore, common ending script for both DATA_FETCH_SUCCEEDED and DATA_FETCH_FAILED must be manually repeated, 
            //  should not be put at the end of the script out side the outer most if else statement
        }
    } else {
        api.setState('isGenAiGenerating', false);
        helpers.timing.clearTimeout(api.state.genAiTimeoutId);

        if (api.state.isGenAiTimedOut) {
            return;
        }

        api.emit("NOW_UXF_PAGE#ADD_NOTIFICATIONS", {
            items: [{
                id: "na_error",
                status: "critical",
                icon: "ai-sparkle-fill",
                content: await helpers.translate("There was a problem generating this article with Now Assist. You can write it yourself now if you'd like."),
                action: {
                    type: "dismiss"
                }
            }]
        });

        api.emit("NOW_UXF_PAGE#REMOVE_NOTIFICATIONS", {
            items: [{
                id: "na_drafting"
            }]
        });
        helpers.modal.close("modal_gen_ai");
    }
}]]></script>
        <script_api_version>2.0.0</script_api_version>
        <sys_class_name>sys_ux_client_script</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-03-20 08:21:49</sys_created_on>
        <sys_id>ab9ddbe883f336102e8dc310feaad3fc</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_name>On Gen AI Data Fetch</sys_name>
        <sys_package display_value="RefurTrace" source="x_1919710_refurt_0">6bd17c5e831b72902e8dc310feaad338</sys_package>
        <sys_policy/>
        <sys_scope display_value="RefurTrace">6bd17c5e831b72902e8dc310feaad338</sys_scope>
        <sys_update_name>sys_ux_client_script_ab9ddbe883f336102e8dc310feaad3fc</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-03-20 08:21:49</sys_updated_on>
        <target>macroponent</target>
        <type>default</type>
    </sys_ux_client_script>
</record_update>
